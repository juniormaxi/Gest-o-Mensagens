import { PrismaClient } from '@prisma/client';import bcrypt from 'bcryptjs';
const prisma=new PrismaClient();const email=process.env.SEED_ADMIN_EMAIL;const password=process.env.SEED_ADMIN_PASSWORD;if(!email||!password||password.length<8)throw new Error('Defina SEED_ADMIN_EMAIL e SEED_ADMIN_PASSWORD (mínimo 8 caracteres)');const passwordHash=await bcrypt.hash(password,12);await prisma.user.upsert({where:{email:email.toLowerCase()},update:{name:'Administrador',passwordHash,role:'ADMIN',active:true},create:{name:'Administrador',email:email.toLowerCase(),passwordHash,role:'ADMIN'}});await prisma.$disconnect();console.log('Administrador de desenvolvimento criado/atualizado.');
